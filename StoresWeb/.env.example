# API base URL (no trailing slash). For production, set in DigitalOcean build env.
VITE_API_BASE_URL=http://localhost:3000

# Optional: only if deployed under a subpath (e.g. /stores/)
# VITE_BASE_PATH=/stores/

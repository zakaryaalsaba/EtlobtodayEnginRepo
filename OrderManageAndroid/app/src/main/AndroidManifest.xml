<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Location Permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />

    <application
        android:name=".OrderManagerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderManageAndroid"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:label="@string/login"
            android:theme="@style/Theme.OrderManageAndroid">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Dashboard Activity -->
        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="false"
            android:label="@string/dashboard"
            android:parentActivityName=".ui.auth.LoginActivity"
            android:theme="@style/Theme.OrderManageAndroid.NoActionBar" />
        
        <!-- Order Detail Activity -->
        <activity
            android:name=".ui.orders.OrderDetailActivity"
            android:exported="false"
            android:label="@string/order_details"
            android:parentActivityName=".ui.dashboard.DashboardActivity"
            android:theme="@style/Theme.OrderManageAndroid.NoActionBar" />
        
        <!-- Statistics Activity -->
        <activity
            android:name=".ui.statistics.StatisticsActivity"
            android:exported="false"
            android:label="@string/statistics"
            android:parentActivityName=".ui.dashboard.DashboardActivity"
            android:theme="@style/Theme.OrderManageAndroid.NoActionBar" />
        
        <!-- Profile Activity -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:label="@string/profile"
            android:parentActivityName=".ui.dashboard.DashboardActivity"
            android:theme="@style/Theme.OrderManageAndroid.NoActionBar" />
        
        <!-- Request Delivery Activity -->
        <activity
            android:name=".ui.delivery.RequestDeliveryActivity"
            android:exported="false"
            android:label="@string/request_delivery"
            android:parentActivityName=".ui.dashboard.DashboardActivity"
            android:theme="@style/Theme.OrderManageAndroid.NoActionBar" />
        
        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".service.OrderFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        
        <!-- Real-time Order Service -->
        <service
            android:name=".service.RealTimeOrderService"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>